// mode_default.h
#ifndef MODE_DEFAULT_H
#define MODE_DEFAULT_H

#include "mode.h"

// Expose the interface implementation for the default mode
extern const RenderModeInterface default_mode_interface;

#endif // MODE_DEFAULT_H
