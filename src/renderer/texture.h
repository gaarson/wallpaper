// mode_texture.h
#ifndef MODE_TEXTURE_H
#define MODE_TEXTURE_H

#include "mode.h"

// Expose the interface implementation for the texture mode
extern const RenderModeInterface texture_mode_interface;

#endif // MODE_TEXTURE_H
